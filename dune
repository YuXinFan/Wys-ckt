(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags -la 2)
)

(executable
  (name wys)
)

(rule
  (with-stdout-to wys.out
  (with-stdin-from wys.in
    (run ./wys.exe)
  ))
)

(rule
  (alias test)
  (action (diff wys.exp wys.out))
)
